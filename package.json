{
  "name": "bihar-assembly-new",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@react-three/drei": "^10.7.4",
    "@react-three/fiber": "^9.3.0",
    "@reduxjs/toolkit": "^2.9.0",
    "@testing-library/dom": "^10.4.1",
    "@testing-library/jest-dom": "^6.8.0",
    "@testing-library/react": "^16.3.0",
    "@testing-library/user-event": "^13.5.0",
    "camera-controls": "^2.7.4",
    "react": "^19.1.1",
    "react-dom": "^19.1.1",
    "react-redux": "^9.2.0",
    "react-scripts": "5.0.1",
    "three": "^0.179.1",
    "web-vitals": "^2.1.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
  "preview": "npx serve -s build -l 3001 --no-clipboard",
  "deploy:preview": "netlify deploy --build --message=preview --dir=build",
  "deploy:prod": "netlify deploy --build --prod --message=production --dir=build",
  "validate:seats": "node scripts/validateSeatLogic.js",
  "lint": "eslint src --ext .js,.jsx",
  "format": "prettier --write \"src/**/*.{js,jsx,css}\"",
  "postinstall": "node scripts/fixMediapipeSourceMap.js || true"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "engines": {
    "node": ">=20 <21"
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0",
    "serve": "^14.2.1"
  }
}
